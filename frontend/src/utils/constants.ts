/** Base URL for all API calls. Swap to env var when deploying. */
export const API_BASE_URL = 'http://localhost:8080/api/v1';

/** Fine accrual rate charged per overdue day (in ₹). */
export const FINE_RATE_PER_DAY = 10;
